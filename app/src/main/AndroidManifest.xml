<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.imagegrafia.androidbasic">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidBasic"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <activity
            android:name=".VerificationActivity"
            android:exported="true" />
        <activity
            android:name=".SignInActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidBasic.NoActionBar" />
        <activity
            android:name=".RazorPaymentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LiveTaskActivity"
            android:exported="false"
            android:label="@string/title_activity_live_task"
            android:theme="@style/Theme.AndroidBasic.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".TestingRestApi" -->
        <!-- android:exported="true" /> -->
        <activity
            android:name=".TestingRestApi"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.AndroidBasic.NoActionBar" />
    </application>

</manifest>